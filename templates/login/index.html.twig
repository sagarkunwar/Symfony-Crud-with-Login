{% extends 'base.html.twig' %}

{# ... #}

{% block body %}
<div class="container my-5 ">
<div class="card text-center">
<article class="card-body">

<div class="card-header">
 Login From Here
</div>

    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
    
    <form method="post">
    <div class="form-group">
        <label for="staticEmail" class="col-sm-2 col-form-label t">Email</label>
      <div class="col-sm-15">  
        <input type="text" id="username" name="_username" value="{{ last_username }}" aria-describedby="emailHelp" placeholder="Enter email"/>
        <br>
        </div>
  </div>
  
    <div class="form-group">
        <label for="inputPassword" class="col-sm-2 col-form-label">Password</label>
        <div class="col-sm-15">      
 
       <input type="password" id="password" name="_password" aria-describedby="emailHelp" placeholder="Enter Password"/>
        </div>
</div>
        <br>
        <br>

        {# If you want to control the URL the user is redirected to on success
        <input type="hidden" name="_target_path" value="/account"/> #}
        
        <button type="submit" class="btn btn-primary">login</button>
        <button type="submit" class="btn btn-danger"><a href="register" >Register</a></button>
        


    </form>
    </article>
    </div>
    </div>
{% endblock %}